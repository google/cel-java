package(
    default_applicable_licenses = [
        "//:license",
    ],
    default_testonly = True,
    default_visibility = [
        "//optimizer:__subpackages__",
    ],
)

filegroup(
    name = "baselines",
    testonly = True,
    srcs = glob(["*.baseline"]),
)
